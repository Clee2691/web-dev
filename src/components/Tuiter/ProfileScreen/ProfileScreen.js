import React from "react";

import ProfileComponent from "./ProfileComponent";

const ProfileScreen = () => {
  return (
    <div className="col">
      <ProfileComponent />
    </div>
  );
};

export default ProfileScreen;
